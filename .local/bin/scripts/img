#!/bin/sh

[ -z "$5" ] && echo "Usage: $0 <image> <x> <y> <max width> <max height>" && exit
source "$(ueberzug library)"

ImageLayer 0< <(
  ImageLayer::add [identifier]="img" [path]="$1" [x]="$2" [y]="$3" [max_width]="$4" [max_height]="$5"
  read
)
