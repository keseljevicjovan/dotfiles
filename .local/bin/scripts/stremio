#!/bin/sh

dunstify "TEST"

ORIG_SAVER_TIMEOUT=$(xset q | grep 'timeout:' | awk '{print $2}')
ORIG_DPMS_STATE=$(xset q | grep 'DPMS is' | awk '{print $3}')

xset s off
xset -dpms
xset s noblank

stremio

xset s $ORIG_SAVER_TIMEOUT

[ "$ORIG_DPMS_STATE" = "Enabled" ] && xset +dpms || xset -dpms
