#!/bin/sh

vm=$(virsh -c qemu:///system list --all --name | grep -v '^$' | dmenu -i -vi -p "Start VM:") && [ -n "$vm" ] && virsh -c qemu:///system start "$vm" && sleep 1 && virt-manager --show-domain-console "$vm" --connect qemu:///system
